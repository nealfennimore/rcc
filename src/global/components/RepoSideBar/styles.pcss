.RepoSideBar {
    grid-area: aside;
    background-color: #DCE1E9;
    padding: 15px;
    box-shadow: 0 0 6px 2px rgba(0, 0, 0, 0.1);

    & > ul {
        list-style-type: none;
        padding-left: 10px;
    }
}
