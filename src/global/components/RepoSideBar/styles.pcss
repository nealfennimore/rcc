.RepoSideBar {
    grid-area: aside;
    background-color: #f6faff;
    padding: 15px;
    box-shadow: 0 0 6px 2px rgba(0, 0, 0, 0.05);

    & > ul {
        list-style-type: none;
        padding-left: 10px;
    }
}
